# Mobile API — `/api/v1`

> **Superseded.** The full, verified contract now lives in
> [`docs/MOBILE_API.md`](./MOBILE_API.md) — it covers every route (including
> streaming chat, voice calls, push tokens, billing portal, and account
> deletion, which this file never listed), the request/response shape for
> each, and a Flutter quick-start. This file is kept only so old links don't
> 404; do not add new content here.
